# Optional remote environment (sourced on the server before write/consolidate).
# Copy to mindsync.env next to your deployment and fill in secrets locally — never commit secrets.

# DATABASE_URL=postgresql://user:pass@localhost:5432/mindsync
